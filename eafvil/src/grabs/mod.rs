// Placeholder modules for future move/resize support.
// Contains unwrap() calls that must be cleaned up before activation.
#[allow(dead_code, clippy::unwrap_used)]
pub mod move_grab;

#[allow(dead_code, clippy::unwrap_used)]
pub mod resize_grab;
